ref: ghcr.io/chainguard-images/testing/release-2.0/curl
versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: curl
      subvariants:
        - suffix: -dev
          options:
            - dev
