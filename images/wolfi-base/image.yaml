ref: ghcr.io/chainguard-images/testing/release-2.0/wolfi-base
terraform: true
versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: curl
      subvariants:
        - suffix: -dev
          options:
            - dev
